# serpay

> turkmen poems

## Build Setup

``` bash
#clone repo
$ git clone [repo url]

# add .env file into ./app folder
$ nano app/.env

# build for production and launch server
$ docker-compose up -d --build
```
